# Список багов и исправлений

1.  **Проблема с диапазонами:**
    - _Было:_ "1½ до 2 литров"
    - _Надо:_ "**от** 1½ до 2 литров"
2.  **Формат градусов:**
    - _Было:_ "190°" (без указания шкалы)
    - _Надо:_ "190°C" (если речь о Цельсии) или перевод F->C. (В оригинале, скорее всего, F).
    - _Решение:_ Проверять контекст (если 350-400° — это F). Конвертировать: `(F-32)*5/9`.
3.  **Неверный перевод терминов:**
    - _Было:_ "ЭКСТРА ВИРГИНСКОЕ ОЛИВКОВОЕ МАСЛО"
    - _Надо:_ "**Оливковое масло первого холодного отжима**" (Extra Virgin Olive Oil).
4.  **Потеря окраски (цвета):**
    - Некоторые заголовки (например, "Storing olive oil") потеряли класс `color_CA4E00` или сам тег `span` при переводе.
    - _Пример:_ Начало 3-го параграфа главы "Extra Virgin Olive Oil".
5.  **Markdown вместо HTML:**
    - _Было:_ `_соффритто_` (курсив через подчеркивание)
    - _Надо:_ `<em>соффритто</em>` (HTML тег).
6.  **Капители (Small Caps):**
    - _Было:_ `ВкУС, В ИтАЛЬЯНСКИХ БЛЮДАХ` (смесь заглавных и маленьких букв, где маленькие — это `class="small"`).
    - _Проблема:_ Выглядит как "прыгающий регистр".
    - _Надо:_ Либо обычный текст (Sentence case: "Вкус, в итальянских блюдах..."), либо обычный Caps Lock ("ВКУС, В ИТАЛЬЯНСКИХ БЛЮДАХ"), убирая класс `small`.
    - _Решение:_ Удалять тег `<span class="small">` внутри заголовков, оставляя текст в верхнем регистре, или приводить к нормальному виду.
